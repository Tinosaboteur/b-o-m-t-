#include <stdio.h>
#include <string.h>

int main(int argc, char *argv[]) {
    char buf[100];
    if (argc < 2) {
        printf("Usage: %s <input>\n", argv[0]);
        return 1;
    }

    strcpy(buf, argv[1]);
    printf("Input was: %s\n", buf);

    return 0;
}
